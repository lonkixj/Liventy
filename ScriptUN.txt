-- Liventy HUB: Преследование цели за спиной сверху, перетаскиваемое окно Quick Targets, Whitelist, ESP, Fly, TP
local Players = game:GetService("Players")
local CoreGui = game:GetService("CoreGui")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")

local localPlayer = Players.LocalPlayer
local camera = workspace.CurrentCamera

-- #bypass_namecall Перехват репортов античита
if hookmetamethod and checkcaller then
    local oldNamecall
    oldNamecall = hookmetamethod(game, "__namecall", function(self, ...)
        local method = getnamecallmethod()
        if not checkcaller() and (method == "FireServer" or method == "InvokeServer") then
            local remoteName = string.lower(self.Name)
            if string.find(remoteName, "cheat") or string.find(remoteName, "ban") or string.find(remoteName, "flag") or string.find(remoteName, "detection") then
                return nil
            end
        end
        return oldNamecall(self, ...)
    end)
end

-- #bypass_gui_container Безопасный родитель
local parentTarget = nil
if gethui then
    parentTarget = gethui()
elseif CoreGui and pcall(function() return CoreGui.Name end) then
    parentTarget = CoreGui
else
    parentTarget = localPlayer:WaitForChild("PlayerGui")
end

if parentTarget:FindFirstChild("LiventyHubGui") then
    parentTarget.LiventyHubGui:Destroy()
end

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "LiventyHubGui"
screenGui.ResetOnSpawn = false
screenGui.Parent = parentTarget

---------------------------------------------------------
-- ПЛАВАЮЩАЯ КНОПКА ОТКРЫТИЯ/ЗАКРЫТИЯ
---------------------------------------------------------
local openToggleButton = Instance.new("TextButton")
openToggleButton.Name = "OpenToggleButton"
openToggleButton.Size = UDim2.new(0, 42, 0, 42)
openToggleButton.Position = UDim2.new(0, 15, 0, 80)
openToggleButton.BackgroundColor3 = Color3.fromRGB(18, 18, 24)
openToggleButton.BackgroundTransparency = 0.35
openToggleButton.Text = ""
openToggleButton.AutoButtonColor = false
openToggleButton.Parent = screenGui

local openToggleCorner = Instance.new("UICorner")
openToggleCorner.CornerRadius = UDim.new(0, 10)
openToggleCorner.Parent = openToggleButton

local openToggleStroke = Instance.new("UIStroke")
openToggleStroke.Thickness = 1.5
openToggleStroke.Color = Color3.fromRGB(50, 110, 255)
openToggleStroke.Parent = openToggleButton

local toggleIcon = Instance.new("ImageLabel")
toggleIcon.Name = "ToggleIcon"
toggleIcon.Size = UDim2.new(0, 24, 0, 24)
toggleIcon.AnchorPoint = Vector2.new(0.5, 0.5)
toggleIcon.Position = UDim2.new(0.5, 0, 0.5, 0)
toggleIcon.BackgroundTransparency = 1
toggleIcon.Image = "rbxassetid://10723415903"
toggleIcon.ImageColor3 = Color3.fromRGB(255, 255, 255)
toggleIcon.Parent = openToggleButton

---------------------------------------------------------
-- ГЛАВНОЕ ОКНО (MAIN FRAME)
---------------------------------------------------------
local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.AnchorPoint = Vector2.new(0.5, 0)
mainFrame.Size = UDim2.new(0, 340, 0, 210)
mainFrame.Position = UDim2.new(0.5, 0, 0.25, 0)
mainFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 24)
mainFrame.BackgroundTransparency = 0.35
mainFrame.BorderSizePixel = 0
mainFrame.ClipsDescendants = true
mainFrame.Parent = screenGui

local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 10)
mainCorner.Parent = mainFrame

local mainGlassStroke = Instance.new("UIStroke")
mainGlassStroke.Thickness = 1
mainGlassStroke.Color = Color3.fromRGB(255, 255, 255)
mainGlassStroke.Transparency = 0.8
mainGlassStroke.Parent = mainFrame

-- Шапка
local headerFrame = Instance.new("Frame")
headerFrame.Name = "HeaderFrame"
headerFrame.Size = UDim2.new(1, 0, 0, 34)
headerFrame.BackgroundTransparency = 1
headerFrame.BorderSizePixel = 0
headerFrame.Parent = mainFrame

local headerDivider = Instance.new("Frame")
headerDivider.Name = "HeaderDivider"
headerDivider.Size = UDim2.new(1, 0, 0, 1)
headerDivider.Position = UDim2.new(0, 0, 0, 34)
headerDivider.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
headerDivider.BackgroundTransparency = 0.85
headerDivider.BorderSizePixel = 0
headerDivider.Parent = headerFrame

local titleLabel = Instance.new("TextLabel")
titleLabel.Name = "TitleLabel"
titleLabel.Size = UDim2.new(0, 160, 1, 0)
titleLabel.Position = UDim2.new(0, 12, 0, 0)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "Liventy HUB"
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
titleLabel.TextSize = 14
titleLabel.Font = Enum.Font.GothamBold
titleLabel.TextXAlignment = Enum.TextXAlignment.Left
titleLabel.Parent = headerFrame

local closeButton = Instance.new("TextButton")
closeButton.Name = "CloseButton"
closeButton.Size = UDim2.new(0, 12, 0, 12)
closeButton.Position = UDim2.new(1, -20, 0.5, -6)
closeButton.BackgroundColor3 = Color3.fromRGB(235, 65, 65)
closeButton.Text = ""
closeButton.AutoButtonColor = false
closeButton.Parent = headerFrame

local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(1, 0)
closeCorner.Parent = closeButton

local minimizeButton = Instance.new("TextButton")
minimizeButton.Name = "MinimizeButton"
minimizeButton.Size = UDim2.new(0, 12, 0, 12)
minimizeButton.Position = UDim2.new(1, -38, 0.5, -6)
minimizeButton.BackgroundColor3 = Color3.fromRGB(50, 205, 80)
minimizeButton.Text = ""
minimizeButton.AutoButtonColor = false
minimizeButton.Parent = headerFrame

local minimizeCorner = Instance.new("UICorner")
minimizeCorner.CornerRadius = UDim.new(1, 0)
minimizeCorner.Parent = minimizeButton

---------------------------------------------------------
-- ЛЕВАЯ БОКОВАЯ ПАНЕЛЬ ВКЛАДОК (SIDEBAR)
---------------------------------------------------------
local sideBar = Instance.new("Frame")
sideBar.Name = "SideBar"
sideBar.Size = UDim2.new(0, 95, 1, -35)
sideBar.Position = UDim2.new(0, 0, 0, 35)
sideBar.BackgroundTransparency = 1
sideBar.BorderSizePixel = 0
sideBar.Parent = mainFrame

local sideBarDivider = Instance.new("Frame")
sideBarDivider.Name = "SideBarDivider"
sideBarDivider.Size = UDim2.new(0, 1, 1, -35)
sideBarDivider.Position = UDim2.new(0, 95, 0, 35)
sideBarDivider.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
sideBarDivider.BackgroundTransparency = 0.85
sideBarDivider.BorderSizePixel = 0
sideBarDivider.Parent = mainFrame

local activeAccent = Instance.new("Frame")
activeAccent.Name = "ActiveAccent"
activeAccent.Size = UDim2.new(0, 3, 0, 14)
activeAccent.Position = UDim2.new(0, 8, 0, 9)
activeAccent.BackgroundColor3 = Color3.fromRGB(50, 110, 255)
activeAccent.BorderSizePixel = 0
activeAccent.ZIndex = 3
activeAccent.Parent = sideBar

local accentCorner = Instance.new("UICorner")
accentCorner.CornerRadius = UDim.new(1, 0)
accentCorner.Parent = activeAccent

local function createTabButton(name, text, positionY)
    local btn = Instance.new("TextButton")
    btn.Name = name
    btn.Size = UDim2.new(1, -12, 0, 22)
    btn.Position = UDim2.new(0, 6, 0, positionY)
    btn.BackgroundColor3 = Color3.fromRGB(28, 30, 42)
    btn.BackgroundTransparency = 0.8
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(180, 185, 200)
    btn.TextSize = 10
    btn.Font = Enum.Font.GothamBold
    btn.Parent = sideBar

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 5)
    corner.Parent = btn

    return btn
end

local playerTabBtn = createTabButton("PlayerTabBtn", "Player", 5)
playerTabBtn.BackgroundTransparency = 0.3
playerTabBtn.TextColor3 = Color3.fromRGB(255, 255, 255)

local visualsTabBtn = createTabButton("VisualsTabBtn", "Visuals", 31)
local combatTabBtn = createTabButton("CombatTabBtn", "Combat", 57)
local tpTabBtn = createTabButton("TpTabBtn", "TP", 83)
local creditsTabBtn = createTabButton("CreditsTabBtn", "Credits", 109)

---------------------------------------------------------
-- КОНТЕЙНЕРЫ ВКЛАДОК
---------------------------------------------------------
local function createContentContainer(name)
    local container = Instance.new("ScrollingFrame")
    container.Name = name
    container.Size = UDim2.new(1, -96, 1, -35)
    container.Position = UDim2.new(0, 96, 0, 35)
    container.BackgroundTransparency = 1
    container.BorderSizePixel = 0
    container.ScrollBarThickness = 3
    container.ScrollBarImageColor3 = Color3.fromRGB(50, 110, 255)
    container.AutomaticCanvasSize = Enum.AutomaticSize.Y
    container.CanvasSize = UDim2.new(0, 0, 0, 0)
    container.ClipsDescendants = true
    container.Parent = mainFrame

    local layout = Instance.new("UIListLayout")
    layout.SortOrder = Enum.SortOrder.LayoutOrder
    layout.Padding = UDim.new(0, 8)
    layout.Parent = container

    local padding = Instance.new("UIPadding")
    padding.PaddingTop = UDim.new(0, 8)
    padding.PaddingLeft = UDim.new(0, 4)
    padding.PaddingRight = UDim.new(0, 8)
    padding.PaddingBottom = UDim.new(0, 8)
    padding.Parent = container

    return container
end

local playerContent = createContentContainer("PlayerContent")
local visualsContent = createContentContainer("VisualsContent")
local combatContent = createContentContainer("CombatContent")
local tpContent = createContentContainer("TpContent")
local creditsContent = createContentContainer("CreditsContent")

visualsContent.Visible = false
combatContent.Visible = false
tpContent.Visible = false
creditsContent.Visible = false

local tweenFast = TweenInfo.new(0.2, Enum.EasingStyle.Quart, Enum.EasingDirection.Out)

local function switchTab(activeBtn, targetContent, accentY)
    playerContent.Visible = (targetContent == playerContent)
    visualsContent.Visible = (targetContent == visualsContent)
    combatContent.Visible = (targetContent == combatContent)
    tpContent.Visible = (targetContent == tpContent)
    creditsContent.Visible = (targetContent == creditsContent)

    local allBtns = {playerTabBtn, visualsTabBtn, combatTabBtn, tpTabBtn, creditsTabBtn}
    for _, btn in ipairs(allBtns) do
        local isActive = (btn == activeBtn)
        btn.BackgroundTransparency = isActive and 0.3 or 0.8
        btn.TextColor3 = isActive and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(180, 185, 200)
    end

    TweenService:Create(activeAccent, tweenFast, {Position = UDim2.new(0, 8, 0, accentY)}):Play()
end

playerTabBtn.MouseButton1Click:Connect(function() switchTab(playerTabBtn, playerContent, 9) end)
visualsTabBtn.MouseButton1Click:Connect(function() switchTab(visualsTabBtn, visualsContent, 35) end)
combatTabBtn.MouseButton1Click:Connect(function() switchTab(combatTabBtn, combatContent, 61) end)
tpTabBtn.MouseButton1Click:Connect(function() switchTab(tpTabBtn, tpContent, 87) end)
creditsTabBtn.MouseButton1Click:Connect(function() switchTab(creditsTabBtn, creditsContent, 113) end)

---------------------------------------------------------
-- КАРТОЧКИ УПРАВЛЕНИЯ
---------------------------------------------------------
local function createToggleCard(parentContainer, titleText, defaultVal)
    local card = Instance.new("Frame")
    card.Name = titleText .. "ToggleCard"
    card.Size = UDim2.new(1, 0, 0, 42)
    card.BackgroundColor3 = Color3.fromRGB(24, 26, 36)
    card.BackgroundTransparency = 0.35
    card.BorderSizePixel = 0
    card.Parent = parentContainer

    local cardCorner = Instance.new("UICorner")
    cardCorner.CornerRadius = UDim.new(0, 8)
    cardCorner.Parent = card

    local cardStroke = Instance.new("UIStroke")
    cardStroke.Thickness = 1
    cardStroke.Color = Color3.fromRGB(255, 255, 255)
    cardStroke.Transparency = 0.85
    cardStroke.Parent = card

    local label = Instance.new("TextLabel")
    label.Name = "Title"
    label.Size = UDim2.new(0, 70, 1, 0)
    label.Position = UDim2.new(0, 8, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = titleText
    label.TextColor3 = Color3.fromRGB(230, 230, 240)
    label.TextSize = 12
    label.Font = Enum.Font.GothamMedium
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = card

    local inputBox = nil
    if defaultVal ~= nil then
        inputBox = Instance.new("TextBox")
        inputBox.Name = "Input"
        inputBox.Size = UDim2.new(0, 40, 0, 22)
        inputBox.Position = UDim2.new(1, -88, 0.5, -11)
        inputBox.BackgroundColor3 = Color3.fromRGB(12, 14, 20)
        inputBox.BackgroundTransparency = 0.3
        inputBox.Text = tostring(defaultVal)
        inputBox.TextColor3 = Color3.fromRGB(255, 255, 255)
        inputBox.TextSize = 11
        inputBox.Font = Enum.Font.GothamBold
        inputBox.Parent = card

        local inputCorner = Instance.new("UICorner")
        inputCorner.CornerRadius = UDim.new(0, 4)
        inputCorner.Parent = inputBox

        local inputStroke = Instance.new("UIStroke")
        inputStroke.Thickness = 1
        inputStroke.Color = Color3.fromRGB(255, 255, 255)
        inputStroke.Transparency = 0.85
        inputStroke.Parent = inputBox
    end

    local switchTrack = Instance.new("TextButton")
    switchTrack.Name = "SwitchTrack"
    switchTrack.Size = UDim2.new(0, 36, 0, 20)
    switchTrack.Position = UDim2.new(1, -42, 0.5, -10)
    switchTrack.BackgroundColor3 = Color3.fromRGB(40, 44, 58)
    switchTrack.Text = ""
    switchTrack.AutoButtonColor = false
    switchTrack.Parent = card

    local trackCorner = Instance.new("UICorner")
    trackCorner.CornerRadius = UDim.new(1, 0)
    trackCorner.Parent = switchTrack

    local switchKnob = Instance.new("Frame")
    switchKnob.Name = "Knob"
    switchKnob.Size = UDim2.new(0, 14, 0, 14)
    switchKnob.Position = UDim2.new(0, 3, 0.5, -7)
    switchKnob.BackgroundColor3 = Color3.fromRGB(180, 185, 200)
    switchKnob.BorderSizePixel = 0
    switchKnob.Parent = switchTrack

    local knobCorner = Instance.new("UICorner")
    knobCorner.CornerRadius = UDim.new(1, 0)
    knobCorner.Parent = switchKnob

    return card, inputBox, switchTrack, switchKnob
end

local function createInputCard(parentContainer, titleText, defaultVal)
    local card = Instance.new("Frame")
    card.Name = titleText .. "InputCard"
    card.Size = UDim2.new(1, 0, 0, 42)
    card.BackgroundColor3 = Color3.fromRGB(24, 26, 36)
    card.BackgroundTransparency = 0.35
    card.BorderSizePixel = 0
    card.Parent = parentContainer

    local cardCorner = Instance.new("UICorner")
    cardCorner.CornerRadius = UDim.new(0, 8)
    cardCorner.Parent = card

    local cardStroke = Instance.new("UIStroke")
    cardStroke.Thickness = 1
    cardStroke.Color = Color3.fromRGB(255, 255, 255)
    cardStroke.Transparency = 0.85
    cardStroke.Parent = card

    local label = Instance.new("TextLabel")
    label.Name = "Title"
    label.Size = UDim2.new(0, 100, 1, 0)
    label.Position = UDim2.new(0, 8, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = titleText
    label.TextColor3 = Color3.fromRGB(230, 230, 240)
    label.TextSize = 12
    label.Font = Enum.Font.GothamMedium
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = card

    local inputBox = Instance.new("TextBox")
    inputBox.Name = "Input"
    inputBox.Size = UDim2.new(0, 45, 0, 22)
    inputBox.Position = UDim2.new(1, -51, 0.5, -11)
    inputBox.BackgroundColor3 = Color3.fromRGB(12, 14, 20)
    inputBox.BackgroundTransparency = 0.3
    inputBox.Text = tostring(defaultVal)
    inputBox.TextColor3 = Color3.fromRGB(255, 255, 255)
    inputBox.TextSize = 11
    inputBox.Font = Enum.Font.GothamBold
    inputBox.Parent = card

    local inputCorner = Instance.new("UICorner")
    inputCorner.CornerRadius = UDim.new(0, 4)
    inputCorner.Parent = inputBox

    local inputStroke = Instance.new("UIStroke")
    inputStroke.Thickness = 1
    inputStroke.Color = Color3.fromRGB(255, 255, 255)
    inputStroke.Transparency = 0.85
    inputStroke.Parent = inputBox

    return card, inputBox
end

local function createInfoCard(parentContainer, titleText, valueText)
    local card = Instance.new("Frame")
    card.Name = titleText .. "InfoCard"
    card.Size = UDim2.new(1, 0, 0, 42)
    card.BackgroundColor3 = Color3.fromRGB(24, 26, 36)
    card.BackgroundTransparency = 0.35
    card.BorderSizePixel = 0
    card.Parent = parentContainer

    local cardCorner = Instance.new("UICorner")
    cardCorner.CornerRadius = UDim.new(0, 8)
    cardCorner.Parent = card

    local cardStroke = Instance.new("UIStroke")
    cardStroke.Thickness = 1
    cardStroke.Color = Color3.fromRGB(255, 255, 255)
    cardStroke.Transparency = 0.85
    cardStroke.Parent = card

    local label = Instance.new("TextLabel")
    label.Name = "Title"
    label.Size = UDim2.new(0, 90, 1, 0)
    label.Position = UDim2.new(0, 8, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = titleText
    label.TextColor3 = Color3.fromRGB(180, 185, 200)
    label.TextSize = 11
    label.Font = Enum.Font.GothamMedium
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = card

    local valLabel = Instance.new("TextLabel")
    valLabel.Name = "Value"
    valLabel.Size = UDim2.new(1, -105, 1, 0)
    valLabel.Position = UDim2.new(0, 98, 0, 0)
    valLabel.BackgroundTransparency = 1
    valLabel.Text = valueText
    valLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    valLabel.TextSize = 11
    valLabel.Font = Enum.Font.GothamBold
    valLabel.TextXAlignment = Enum.TextXAlignment.Right
    valLabel.Parent = card

    return card, valLabel
end

-- Элементы Player
local speedCard, speedInput, speedSwitch, speedKnob = createToggleCard(playerContent, "Speed", 50)
local flyCard, flySpeedInput, flySwitch, flyKnob = createToggleCard(playerContent, "Fly", 50)
local noClipCard, _, noClipSwitch, noClipKnob = createToggleCard(playerContent, "NoClip", nil)
local jumpCard, jumpInput, jumpSwitch, jumpKnob = createToggleCard(playerContent, "Jump", 50)

-- Элементы Visuals
local espCard, _, espSwitch, espKnob = createToggleCard(visualsContent, "ESP", nil)
local distCard, espDistInput = createInputCard(visualsContent, "Max Dist", 300)

---------------------------------------------------------
-- COMBAT И ПЕРЕТАСКИВАЕМОЕ ОКНО QUICK TARGETS
---------------------------------------------------------
local selectedCombatPlayer = nil
local isCombatFollowActive = false
local isInstantKillActive = false
local whitelistedPlayers = {}

local killCard, _, killSwitch, killKnob = createToggleCard(combatContent, "Target Panel", nil)

local whitelistCard = Instance.new("Frame")
whitelistCard.Name = "WhitelistCard"
whitelistCard.Size = UDim2.new(1, 0, 0, 85)
whitelistCard.BackgroundColor3 = Color3.fromRGB(24, 26, 36)
whitelistCard.BackgroundTransparency = 0.35
whitelistCard.BorderSizePixel = 0
whitelistCard.Parent = combatContent

local whitelistCorner = Instance.new("UICorner")
whitelistCorner.CornerRadius = UDim.new(0, 8)
whitelistCorner.Parent = whitelistCard

local whitelistStroke = Instance.new("UIStroke")
whitelistStroke.Thickness = 1
whitelistStroke.Color = Color3.fromRGB(255, 255, 255)
whitelistStroke.Transparency = 0.85
whitelistStroke.Parent = whitelistCard

local whitelistLabel = Instance.new("TextLabel")
whitelistLabel.Name = "WhitelistLabel"
whitelistLabel.Size = UDim2.new(1, -12, 0, 18)
whitelistLabel.Position = UDim2.new(0, 8, 0, 4)
whitelistLabel.BackgroundTransparency = 1
whitelistLabel.Text = "Whitelist (Safe Players):"
whitelistLabel.TextColor3 = Color3.fromRGB(230, 230, 240)
whitelistLabel.TextSize = 12
whitelistLabel.Font = Enum.Font.GothamMedium
whitelistLabel.TextXAlignment = Enum.TextXAlignment.Left
whitelistLabel.Parent = whitelistCard

local whitelistList = Instance.new("ScrollingFrame")
whitelistList.Name = "WhitelistList"
whitelistList.Size = UDim2.new(1, -16, 0, 55)
whitelistList.Position = UDim2.new(0, 8, 0, 24)
whitelistList.BackgroundColor3 = Color3.fromRGB(12, 14, 20)
whitelistList.BackgroundTransparency = 0.3
whitelistList.BorderSizePixel = 0
whitelistList.ScrollBarThickness = 2
whitelistList.ScrollBarImageColor3 = Color3.fromRGB(50, 110, 255)
whitelistList.AutomaticCanvasSize = Enum.AutomaticSize.Y
whitelistList.CanvasSize = UDim2.new(0, 0, 0, 0)
whitelistList.Parent = whitelistCard

local whitelistListCorner = Instance.new("UICorner")
whitelistListCorner.CornerRadius = UDim.new(0, 5)
whitelistListCorner.Parent = whitelistList

local whitelistLayout = Instance.new("UIListLayout")
whitelistLayout.SortOrder = Enum.SortOrder.LayoutOrder
whitelistLayout.Padding = UDim.new(0, 3)
whitelistLayout.Parent = whitelistList

---------------------------------------------------------
-- ПЕРЕТАСКИВАЕМОЕ И СВОРАЧИВАЕМОЕ ОКНО QUICK TARGETS
---------------------------------------------------------
local quickTargetFrame = Instance.new("Frame")
quickTargetFrame.Name = "QuickTargetFrame"
quickTargetFrame.Size = UDim2.new(0, 150, 0, 140)
quickTargetFrame.Position = UDim2.new(0, 15, 0, 130)
quickTargetFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 24)
quickTargetFrame.BackgroundTransparency = 0.35
quickTargetFrame.BorderSizePixel = 0
quickTargetFrame.Visible = false
quickTargetFrame.ClipsDescendants = true
quickTargetFrame.Parent = screenGui

local quickCorner = Instance.new("UICorner")
quickCorner.CornerRadius = UDim.new(0, 8)
quickCorner.Parent = quickTargetFrame

local quickStroke = Instance.new("UIStroke")
quickStroke.Thickness = 1.5
quickStroke.Color = Color3.fromRGB(50, 110, 255)
quickStroke.Parent = quickTargetFrame

local quickHeader = Instance.new("Frame")
quickHeader.Name = "QuickHeader"
quickHeader.Size = UDim2.new(1, 0, 0, 26)
quickHeader.BackgroundTransparency = 1
quickHeader.Parent = quickTargetFrame

local quickTitle = Instance.new("TextLabel")
quickTitle.Name = "QuickTitle"
quickTitle.Size = UDim2.new(1, -30, 1, 0)
quickTitle.Position = UDim2.new(0, 8, 0, 0)
quickTitle.BackgroundTransparency = 1
quickTitle.Text = "Quick Targets"
quickTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
quickTitle.TextSize = 11
quickTitle.Font = Enum.Font.GothamBold
quickTitle.TextXAlignment = Enum.TextXAlignment.Left
quickTitle.Parent = quickHeader

-- Кнопка свертывания оверлея Quick Targets
local quickMinBtn = Instance.new("TextButton")
quickMinBtn.Name = "QuickMinBtn"
quickMinBtn.Size = UDim2.new(0, 16, 0, 16)
quickMinBtn.Position = UDim2.new(1, -22, 0.5, -8)
quickMinBtn.BackgroundColor3 = Color3.fromRGB(50, 205, 80)
quickMinBtn.Text = ""
quickMinBtn.AutoButtonColor = false
quickMinBtn.Parent = quickHeader

local quickMinCorner = Instance.new("UICorner")
quickMinCorner.CornerRadius = UDim.new(1, 0)
quickMinCorner.Parent = quickMinBtn

local quickDivider = Instance.new("Frame")
quickDivider.Name = "QuickDivider"
quickDivider.Size = UDim2.new(1, 0, 0, 1)
quickDivider.Position = UDim2.new(0, 0, 0, 26)
quickDivider.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
quickDivider.BackgroundTransparency = 0.85
quickDivider.BorderSizePixel = 0
quickDivider.Parent = quickTargetFrame

local quickScroll = Instance.new("ScrollingFrame")
quickScroll.Name = "QuickScroll"
quickScroll.Size = UDim2.new(1, -10, 1, -32)
quickScroll.Position = UDim2.new(0, 5, 0, 29)
quickScroll.BackgroundTransparency = 1
quickScroll.BorderSizePixel = 0
quickScroll.ScrollBarThickness = 2
quickScroll.ScrollBarImageColor3 = Color3.fromRGB(50, 110, 255)
quickScroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
quickScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
quickScroll.Parent = quickTargetFrame

local quickLayout = Instance.new("UIListLayout")
quickLayout.SortOrder = Enum.SortOrder.LayoutOrder
quickLayout.Padding = UDim.new(0, 4)
quickLayout.Parent = quickScroll

-- Функция перетаскивания (Drag) для Quick Targets
local qDragging = false
local qDragStart, qStartPos

quickHeader.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        qDragging = true
        qDragStart = input.Position
        qStartPos = quickTargetFrame.Position

        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                qDragging = false
            end
        end)
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if qDragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - qDragStart
        quickTargetFrame.Position = UDim2.new(
            qStartPos.X.Scale,
            qStartPos.X.Offset + delta.X,
            qStartPos.Y.Scale,
            qStartPos.Y.Offset + delta.Y
        )
    end
end)

-- Функция свертывания Quick Targets
local isQuickMinimized = false
quickMinBtn.MouseButton1Click:Connect(function()
    isQuickMinimized = not isQuickMinimized
    if isQuickMinimized then
        TweenService:Create(quickTargetFrame, tweenFast, {Size = UDim2.new(0, 150, 0, 26)}):Play()
    else
        TweenService:Create(quickTargetFrame, tweenFast, {Size = UDim2.new(0, 150, 0, 140)}):Play()
    end
end)

-- Обновление списков игроков в Whitelist и Quick Targets
local function refreshCombatPlayerLists()
    for _, item in ipairs(whitelistList:GetChildren()) do
        if item:IsA("TextButton") then item:Destroy() end
    end
    for _, item in ipairs(quickScroll:GetChildren()) do
        if item:IsA("TextButton") then item:Destroy() end
    end

    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= localPlayer then
            local isWl = whitelistedPlayers[player] == true

            local wlBtn = Instance.new("TextButton")
            wlBtn.Name = "Wl_" .. player.Name
            wlBtn.Size = UDim2.new(1, -6, 0, 20)
            wlBtn.BackgroundColor3 = isWl and Color3.fromRGB(50, 110, 255) or Color3.fromRGB(28, 30, 42)
            wlBtn.BackgroundTransparency = 0.3
            wlBtn.Text = " [" .. (isWl and "SAFE" or "TARGET") .. "] " .. player.DisplayName
            wlBtn.TextColor3 = isWl and Color3.fromRGB(120, 255, 140) or Color3.fromRGB(220, 220, 220)
            wlBtn.TextSize = 10
            wlBtn.Font = Enum.Font.Gotham
            wlBtn.TextXAlignment = Enum.TextXAlignment.Left
            wlBtn.Parent = whitelistList

            local wlCorner = Instance.new("UICorner")
            wlCorner.CornerRadius = UDim.new(0, 4)
            wlCorner.Parent = wlBtn

            wlBtn.MouseButton1Click:Connect(function()
                whitelistedPlayers[player] = not whitelistedPlayers[player]
                refreshCombatPlayerLists()
            end)

            if not isWl then
                local isSelected = (selectedCombatPlayer == player and isCombatFollowActive)
                local qBtn = Instance.new("TextButton")
                qBtn.Name = "QTarget_" .. player.Name
                qBtn.Size = UDim2.new(1, -4, 0, 22)
                qBtn.BackgroundColor3 = isSelected and Color3.fromRGB(50, 110, 255) or Color3.fromRGB(28, 30, 42)
                qBtn.BackgroundTransparency = 0.2
                qBtn.Text = " " .. player.DisplayName
                qBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
                qBtn.TextSize = 10
                qBtn.Font = Enum.Font.GothamBold
                qBtn.TextXAlignment = Enum.TextXAlignment.Left
                qBtn.Parent = quickScroll

                local qCorner = Instance.new("UICorner")
                qCorner.CornerRadius = UDim.new(0, 4)
                qCorner.Parent = qBtn

                local qStroke = Instance.new("UIStroke")
                qStroke.Thickness = 1
                qStroke.Color = isSelected and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(255, 60, 60)
                qStroke.Parent = qBtn

                -- Выбор и преследование цели в 1 клик
                qBtn.MouseButton1Click:Connect(function()
                    if selectedCombatPlayer == player and isCombatFollowActive then
                        isCombatFollowActive = false
                        selectedCombatPlayer = nil
                    else
                        selectedCombatPlayer = player
                        isCombatFollowActive = true
                    end
                    refreshCombatPlayerLists()
                end)
            end
        end
    end
end

Players.PlayerAdded:Connect(refreshCombatPlayerLists)
Players.PlayerRemoving:Connect(refreshCombatPlayerLists)
refreshCombatPlayerLists()

killSwitch.MouseButton1Click:Connect(function()
    isInstantKillActive = not isInstantKillActive
    quickTargetFrame.Visible = isInstantKillActive
    if isInstantKillActive then
        refreshCombatPlayerLists()
        TweenService:Create(killSwitch, tweenFast, {BackgroundColor3 = Color3.fromRGB(50, 110, 255)}):Play()
        TweenService:Create(killKnob, tweenFast, {Position = UDim2.new(1, -17, 0.5, -7), BackgroundColor3 = Color3.fromRGB(255, 255, 255)}):Play()
    else
        isCombatFollowActive = false
        TweenService:Create(killSwitch, tweenFast, {BackgroundColor3 = Color3.fromRGB(40, 44, 58)}):Play()
        TweenService:Create(killKnob, tweenFast, {Position = UDim2.new(0, 3, 0.5, -7), BackgroundColor3 = Color3.fromRGB(180, 185, 200)}):Play()
    end
end)

---------------------------------------------------------
-- ЭЛЕМЕНТЫ ВКЛАДКИ TP
---------------------------------------------------------
local selectedTpPlayer = nil
local savedWaypointCFrame = nil

local tpSelectCard = Instance.new("Frame")
tpSelectCard.Name = "TpSelectCard"
tpSelectCard.Size = UDim2.new(1, 0, 0, 80)
tpSelectCard.BackgroundColor3 = Color3.fromRGB(24, 26, 36)
tpSelectCard.BackgroundTransparency = 0.35
tpSelectCard.BorderSizePixel = 0
tpSelectCard.Parent = tpContent

local tpSelectCorner = Instance.new("UICorner")
tpSelectCorner.CornerRadius = UDim.new(0, 8)
tpSelectCorner.Parent = tpSelectCard

local tpSelectStroke = Instance.new("UIStroke")
tpSelectStroke.Thickness = 1
tpSelectStroke.Color = Color3.fromRGB(255, 255, 255)
tpSelectStroke.Transparency = 0.85
tpSelectStroke.Parent = tpSelectCard

local tpSelectLabel = Instance.new("TextLabel")
tpSelectLabel.Name = "TpSelectLabel"
tpSelectLabel.Size = UDim2.new(1, -12, 0, 18)
tpSelectLabel.Position = UDim2.new(0, 8, 0, 4)
tpSelectLabel.BackgroundTransparency = 1
tpSelectLabel.Text = "Target Player:"
tpSelectLabel.TextColor3 = Color3.fromRGB(230, 230, 240)
tpSelectLabel.TextSize = 12
tpSelectLabel.Font = Enum.Font.GothamMedium
tpSelectLabel.TextXAlignment = Enum.TextXAlignment.Left
tpSelectLabel.Parent = tpSelectCard

local tpPlayerList = Instance.new("ScrollingFrame")
tpPlayerList.Name = "TpPlayerList"
tpPlayerList.Size = UDim2.new(1, -16, 0, 50)
tpPlayerList.Position = UDim2.new(0, 8, 0, 24)
tpPlayerList.BackgroundColor3 = Color3.fromRGB(12, 14, 20)
tpPlayerList.BackgroundTransparency = 0.3
tpPlayerList.BorderSizePixel = 0
tpPlayerList.ScrollBarThickness = 2
tpPlayerList.ScrollBarImageColor3 = Color3.fromRGB(50, 110, 255)
tpPlayerList.AutomaticCanvasSize = Enum.AutomaticSize.Y
tpPlayerList.CanvasSize = UDim2.new(0, 0, 0, 0)
tpPlayerList.Parent = tpSelectCard

local tpListCorner = Instance.new("UICorner")
tpListCorner.CornerRadius = UDim.new(0, 5)
tpListCorner.Parent = tpPlayerList

local tpListLayout = Instance.new("UIListLayout")
tpListLayout.SortOrder = Enum.SortOrder.LayoutOrder
tpListLayout.Padding = UDim.new(0, 3)
tpListLayout.Parent = tpPlayerList

local tpPlayerActionCard = Instance.new("Frame")
tpPlayerActionCard.Name = "TpPlayerActionCard"
tpPlayerActionCard.Size = UDim2.new(1, 0, 0, 42)
tpPlayerActionCard.BackgroundColor3 = Color3.fromRGB(24, 26, 36)
tpPlayerActionCard.BackgroundTransparency = 0.35
tpPlayerActionCard.BorderSizePixel = 0
tpPlayerActionCard.Parent = tpContent

local tpPlayerActionCorner = Instance.new("UICorner")
tpPlayerActionCorner.CornerRadius = UDim.new(0, 8)
tpPlayerActionCorner.Parent = tpPlayerActionCard

local tpPlayerActionStroke = Instance.new("UIStroke")
tpPlayerActionStroke.Thickness = 1
tpPlayerActionStroke.Color = Color3.fromRGB(255, 255, 255)
tpPlayerActionStroke.Transparency = 0.85
tpPlayerActionStroke.Parent = tpPlayerActionCard

local tpPlayerBtn = Instance.new("TextButton")
tpPlayerBtn.Name = "TpPlayerBtn"
tpPlayerBtn.Size = UDim2.new(1, -16, 0, 26)
tpPlayerBtn.Position = UDim2.new(0, 8, 0.5, -13)
tpPlayerBtn.BackgroundColor3 = Color3.fromRGB(28, 30, 42)
tpPlayerBtn.BackgroundTransparency = 0.2
tpPlayerBtn.Text = "Teleport To Player"
tpPlayerBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
tpPlayerBtn.TextSize = 11
tpPlayerBtn.Font = Enum.Font.GothamBold
tpPlayerBtn.AutoButtonColor = false
tpPlayerBtn.Parent = tpPlayerActionCard

local tpPlayerBtnCorner = Instance.new("UICorner")
tpPlayerBtnCorner.CornerRadius = UDim.new(0, 6)
tpPlayerBtnCorner.Parent = tpPlayerBtn

local tpPlayerBtnStroke = Instance.new("UIStroke")
tpPlayerBtnStroke.Thickness = 1
tpPlayerBtnStroke.Color = Color3.fromRGB(255, 255, 255)
tpPlayerBtnStroke.Transparency = 0.85
tpPlayerBtnStroke.Parent = tpPlayerBtn

local waypointCard = Instance.new("Frame")
waypointCard.Name = "WaypointCard"
waypointCard.Size = UDim2.new(1, 0, 0, 70)
waypointCard.BackgroundColor3 = Color3.fromRGB(24, 26, 36)
waypointCard.BackgroundTransparency = 0.35
waypointCard.BorderSizePixel = 0
waypointCard.Parent = tpContent

local waypointCorner = Instance.new("UICorner")
waypointCorner.CornerRadius = UDim.new(0, 8)
waypointCorner.Parent = waypointCard

local waypointStroke = Instance.new("UIStroke")
waypointStroke.Thickness = 1
waypointStroke.Color = Color3.fromRGB(255, 255, 255)
waypointStroke.Transparency = 0.85
waypointStroke.Parent = waypointCard

local waypointStatusLabel = Instance.new("TextLabel")
waypointStatusLabel.Name = "WaypointStatusLabel"
waypointStatusLabel.Size = UDim2.new(1, -12, 0, 18)
waypointStatusLabel.Position = UDim2.new(0, 8, 0, 4)
waypointStatusLabel.BackgroundTransparency = 1
waypointStatusLabel.Text = "Saved Point: None"
waypointStatusLabel.TextColor3 = Color3.fromRGB(200, 205, 220)
waypointStatusLabel.TextSize = 11
waypointStatusLabel.Font = Enum.Font.GothamMedium
waypointStatusLabel.TextXAlignment = Enum.TextXAlignment.Left
waypointStatusLabel.Parent = waypointCard

local savePointBtn = Instance.new("TextButton")
savePointBtn.Name = "SavePointBtn"
savePointBtn.Size = UDim2.new(0.48, -6, 0, 26)
savePointBtn.Position = UDim2.new(0, 8, 0, 36)
savePointBtn.BackgroundColor3 = Color3.fromRGB(28, 30, 42)
savePointBtn.BackgroundTransparency = 0.2
savePointBtn.Text = "Save Point"
savePointBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
savePointBtn.TextSize = 11
savePointBtn.Font = Enum.Font.GothamBold
savePointBtn.AutoButtonColor = false
savePointBtn.Parent = waypointCard

local savePointCorner = Instance.new("UICorner")
savePointCorner.CornerRadius = UDim.new(0, 6)
savePointCorner.Parent = savePointBtn

local savePointStroke = Instance.new("UIStroke")
savePointStroke.Thickness = 1
savePointStroke.Color = Color3.fromRGB(255, 255, 255)
savePointStroke.Transparency = 0.85
savePointStroke.Parent = savePointBtn

local tpPointBtn = Instance.new("TextButton")
tpPointBtn.Name = "TpPointBtn"
tpPointBtn.Size = UDim2.new(0.48, -6, 0, 26)
tpPointBtn.Position = UDim2.new(0.52, 2, 0, 36)
tpPointBtn.BackgroundColor3 = Color3.fromRGB(28, 30, 42)
tpPointBtn.BackgroundTransparency = 0.2
tpPointBtn.Text = "TP To Point"
tpPointBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
tpPointBtn.TextSize = 11
tpPointBtn.Font = Enum.Font.GothamBold
tpPointBtn.AutoButtonColor = false
tpPointBtn.Parent = waypointCard

local tpPointCorner = Instance.new("UICorner")
tpPointCorner.CornerRadius = UDim.new(0, 6)
tpPointCorner.Parent = tpPointBtn

local tpPointStroke = Instance.new("UIStroke")
tpPointStroke.Thickness = 1
tpPointStroke.Color = Color3.fromRGB(255, 255, 255)
tpPointStroke.Transparency = 0.85
tpPointStroke.Parent = tpPointBtn

---------------------------------------------------------
-- ЭЛЕМЕНТЫ ВКЛАДКИ CREDITS
---------------------------------------------------------
createInfoCard(creditsContent, "Author", "Александр")
createInfoCard(creditsContent, "TG Channel", "t.me/LysteYK")
createInfoCard(creditsContent, "TG User", "@txtFileT")

local function refreshTpPlayerList()
    for _, item in ipairs(tpPlayerList:GetChildren()) do
        if item:IsA("TextButton") then item:Destroy() end
    end

    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= localPlayer then
            local btn = Instance.new("TextButton")
            btn.Name = "Player_" .. player.Name
            btn.Size = UDim2.new(1, -6, 0, 20)
            btn.BackgroundColor3 = (selectedTpPlayer == player) and Color3.fromRGB(50, 110, 255) or Color3.fromRGB(28, 30, 42)
            btn.BackgroundTransparency = 0.3
            btn.Text = " " .. player.DisplayName .. " (@" .. player.Name .. ")"
            btn.TextColor3 = Color3.fromRGB(255, 255, 255)
            btn.TextSize = 10
            btn.Font = Enum.Font.Gotham
            btn.TextXAlignment = Enum.TextXAlignment.Left
            btn.Parent = tpPlayerList

            local btnCorner = Instance.new("UICorner")
            btnCorner.CornerRadius = UDim.new(0, 4)
            btnCorner.Parent = btn

            btn.MouseButton1Click:Connect(function()
                selectedTpPlayer = player
                refreshTpPlayerList()
            end)
        end
    end
end

Players.PlayerAdded:Connect(refreshTpPlayerList)
Players.PlayerRemoving:Connect(refreshTpPlayerList)
refreshTpPlayerList()

tpPlayerBtn.MouseButton1Click:Connect(function()
    if selectedTpPlayer and selectedTpPlayer.Character then
        local targetHrp = selectedTpPlayer.Character:FindFirstChild("HumanoidRootPart")
        local myChar = localPlayer.Character
        local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")

        if targetHrp and myHrp then
            myHrp.AssemblyLinearVelocity = Vector3.zero
            myHrp.CFrame = targetHrp.CFrame * CFrame.new(0, 0, 3)
            task.wait(0.05)
            myHrp.AssemblyLinearVelocity = Vector3.zero
        end
    end
end)

savePointBtn.MouseButton1Click:Connect(function()
    local myChar = localPlayer.Character
    local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
    if myHrp then
        savedWaypointCFrame = myHrp.CFrame
        waypointStatusLabel.Text = "Saved Point: Set"
        waypointStatusLabel.TextColor3 = Color3.fromRGB(100, 255, 120)
    end
end)

tpPointBtn.MouseButton1Click:Connect(function()
    if savedWaypointCFrame and localPlayer.Character then
        local myHrp = localPlayer.Character:FindFirstChild("HumanoidRootPart")
        if myHrp then
            myHrp.AssemblyLinearVelocity = Vector3.zero
            myHrp.CFrame = savedWaypointCFrame
            task.wait(0.05)
            myHrp.AssemblyLinearVelocity = Vector3.zero
        end
    end
end)

-- Палитра 8 цветов
local colorCard = Instance.new("Frame")
colorCard.Name = "EspColorCard"
colorCard.Size = UDim2.new(1, 0, 0, 52)
colorCard.BackgroundColor3 = Color3.fromRGB(24, 26, 36)
colorCard.BackgroundTransparency = 0.35
colorCard.BorderSizePixel = 0
colorCard.Parent = visualsContent

local colorCardCorner = Instance.new("UICorner")
colorCardCorner.CornerRadius = UDim.new(0, 8)
colorCardCorner.Parent = colorCard

local colorCardStroke = Instance.new("UIStroke")
colorCardStroke.Thickness = 1
colorCardStroke.Color = Color3.fromRGB(255, 255, 255)
colorCardStroke.Transparency = 0.85
colorCardStroke.Parent = colorCard

local colorLabel = Instance.new("TextLabel")
colorLabel.Name = "ColorLabel"
colorLabel.Size = UDim2.new(1, -12, 0, 18)
colorLabel.Position = UDim2.new(0, 8, 0, 4)
colorLabel.BackgroundTransparency = 1
colorLabel.Text = "ESP Color"
colorLabel.TextColor3 = Color3.fromRGB(230, 230, 240)
colorLabel.TextSize = 12
colorLabel.Font = Enum.Font.GothamMedium
colorLabel.TextXAlignment = Enum.TextXAlignment.Left
colorLabel.Parent = colorCard

local paletteFrame = Instance.new("Frame")
paletteFrame.Name = "PaletteFrame"
paletteFrame.Size = UDim2.new(1, -16, 0, 22)
paletteFrame.Position = UDim2.new(0, 8, 0, 24)
paletteFrame.BackgroundTransparency = 1
paletteFrame.Parent = colorCard

local paletteLayout = Instance.new("UIListLayout")
paletteLayout.FillDirection = Enum.FillDirection.Horizontal
paletteLayout.HorizontalAlignment = Enum.HorizontalAlignment.Left
paletteLayout.SortOrder = Enum.SortOrder.LayoutOrder
paletteLayout.Padding = UDim.new(0, 6)
paletteLayout.Parent = paletteFrame

local standardColors = {
    Color3.fromRGB(235, 65, 65), Color3.fromRGB(50, 215, 75), Color3.fromRGB(50, 110, 255),
    Color3.fromRGB(255, 210, 40), Color3.fromRGB(0, 220, 245), Color3.fromRGB(240, 70, 220),
    Color3.fromRGB(255, 255, 255), Color3.fromRGB(255, 130, 30)
}

local currentEspColor = standardColors[1]

for index, colorVal in ipairs(standardColors) do
    local colorBtn = Instance.new("TextButton")
    colorBtn.Name = "ColorBtn_" .. index
    colorBtn.Size = UDim2.new(0, 20, 0, 20)
    colorBtn.BackgroundColor3 = colorVal
    colorBtn.Text = ""
    colorBtn.AutoButtonColor = false
    colorBtn.Parent = paletteFrame

    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(1, 0)
    btnCorner.Parent = colorBtn

    local btnStroke = Instance.new("UIStroke")
    btnStroke.Thickness = (index == 1) and 2 or 1
    btnStroke.Color = (index == 1) and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(0, 0, 0)
    btnStroke.Parent = colorBtn

    colorBtn.MouseButton1Click:Connect(function()
        currentEspColor = colorVal
        for _, sibling in ipairs(paletteFrame:GetChildren()) do
            if sibling:IsA("TextButton") then
                sibling.UIStroke.Thickness = 1
                sibling.UIStroke.Color = Color3.fromRGB(0, 0, 0)
            end
        end
        btnStroke.Thickness = 2
        btnStroke.Color = Color3.fromRGB(255, 255, 255)
    end)
end

---------------------------------------------------------
-- МОБИЛЬНЫЙ ДЖОЙСТИК ДЛЯ FLY
---------------------------------------------------------
local joystickFrame = Instance.new("Frame")
joystickFrame.Name = "FlyJoystickFrame"
joystickFrame.Size = UDim2.new(0, 90, 0, 90)
joystickFrame.Position = UDim2.new(0, 15, 1, -115)
joystickFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 24)
joystickFrame.BackgroundTransparency = 0.3
joystickFrame.Visible = false
joystickFrame.Parent = screenGui

local joystickCorner = Instance.new("UICorner")
joystickCorner.CornerRadius = UDim.new(1, 0)
joystickCorner.Parent = joystickFrame

local joystickStroke = Instance.new("UIStroke")
joystickStroke.Thickness = 2
joystickStroke.Color = Color3.fromRGB(50, 110, 255)
joystickStroke.Parent = joystickFrame

local joystickKnob = Instance.new("Frame")
joystickKnob.Name = "JoystickKnob"
joystickKnob.Size = UDim2.new(0, 36, 0, 36)
joystickKnob.AnchorPoint = Vector2.new(0.5, 0.5)
joystickKnob.Position = UDim2.new(0.5, 0, 0.5, 0)
joystickKnob.BackgroundColor3 = Color3.fromRGB(50, 110, 255)
joystickKnob.Parent = joystickFrame

local knobCorner = Instance.new("UICorner")
knobCorner.CornerRadius = UDim.new(1, 0)
knobCorner.Parent = joystickKnob

---------------------------------------------------------
-- ЛОГИКА ВКЛЮЧЕНИЯ/ВЫКЛЮЧЕНИЯ НИКОВ ROBLOX
---------------------------------------------------------
local function updateRobloxNametags(espActive)
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= localPlayer and player.Character then
            local humanoid = player.Character:FindFirstChildOfClass("Humanoid")
            if humanoid then
                if espActive then
                    humanoid.DisplayDistanceType = Enum.HumanoidDisplayDistanceType.None
                else
                    humanoid.DisplayDistanceType = Enum.HumanoidDisplayDistanceType.Viewer
                end
            end
        end
    end
end

---------------------------------------------------------
-- ЛОГИКА СВИТЧЕРОВ И УПРАВЛЕНИЯ
---------------------------------------------------------
local isSpeedEnabled = false
local isFlyEnabled = false
local isNoClipEnabled = false
local isJumpEnabled = false
local isEspEnabled = false

local moveVector = Vector2.zero
local joystickDragging = false
local joystickTouchInput = nil

local function updateJoystick(inputPosition)
    local center = joystickFrame.AbsolutePosition + (joystickFrame.AbsoluteSize / 2)
    local delta = inputPosition - center
    local radius = joystickFrame.AbsoluteSize.X / 2
    local distance = delta.Magnitude

    if distance > radius then
        delta = delta.Unit * radius
    end

    joystickKnob.Position = UDim2.new(0.5, delta.X, 0.5, delta.Y)
    moveVector = delta / radius
end

joystickFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
        joystickDragging = true
        joystickTouchInput = input
        updateJoystick(Vector2.new(input.Position.X, input.Position.Y))
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if joystickDragging and (input == joystickTouchInput or input.UserInputType == Enum.UserInputType.MouseMovement) then
        updateJoystick(Vector2.new(input.Position.X, input.Position.Y))
    end
end)

local function resetJoystick()
    joystickDragging = false
    joystickTouchInput = nil
    moveVector = Vector2.zero
    joystickKnob.Position = UDim2.new(0.5, 0, 0.5, 0)
end

UserInputService.InputEnded:Connect(function(input)
    if input == joystickTouchInput or input.UserInputType == Enum.UserInputType.MouseButton1 then
        resetJoystick()
    end
end)

speedSwitch.MouseButton1Click:Connect(function()
    isSpeedEnabled = not isSpeedEnabled
    if isSpeedEnabled then
        TweenService:Create(speedSwitch, tweenFast, {BackgroundColor3 = Color3.fromRGB(50, 110, 255)}):Play()
        TweenService:Create(speedKnob, tweenFast, {Position = UDim2.new(1, -17, 0.5, -7), BackgroundColor3 = Color3.fromRGB(255, 255, 255)}):Play()
    else
        TweenService:Create(speedSwitch, tweenFast, {BackgroundColor3 = Color3.fromRGB(40, 44, 58)}):Play()
        TweenService:Create(speedKnob, tweenFast, {Position = UDim2.new(0, 3, 0.5, -7), BackgroundColor3 = Color3.fromRGB(180, 185, 200)}):Play()
    end
end)

flySwitch.MouseButton1Click:Connect(function()
    isFlyEnabled = not isFlyEnabled
    joystickFrame.Visible = isFlyEnabled
    if isFlyEnabled then
        TweenService:Create(flySwitch, tweenFast, {BackgroundColor3 = Color3.fromRGB(50, 110, 255)}):Play()
        TweenService:Create(flyKnob, tweenFast, {Position = UDim2.new(1, -17, 0.5, -7), BackgroundColor3 = Color3.fromRGB(255, 255, 255)}):Play()
    else
        TweenService:Create(flySwitch, tweenFast, {BackgroundColor3 = Color3.fromRGB(40, 44, 58)}):Play()
        TweenService:Create(flyKnob, tweenFast, {Position = UDim2.new(0, 3, 0.5, -7), BackgroundColor3 = Color3.fromRGB(180, 185, 200)}):Play()
        resetJoystick()
    end
end)

noClipSwitch.MouseButton1Click:Connect(function()
    isNoClipEnabled = not isNoClipEnabled
    if isNoClipEnabled then
        TweenService:Create(noClipSwitch, tweenFast, {BackgroundColor3 = Color3.fromRGB(50, 110, 255)}):Play()
        TweenService:Create(noClipKnob, tweenFast, {Position = UDim2.new(1, -17, 0.5, -7), BackgroundColor3 = Color3.fromRGB(255, 255, 255)}):Play()
    else
        TweenService:Create(noClipSwitch, tweenFast, {BackgroundColor3 = Color3.fromRGB(40, 44, 58)}):Play()
        TweenService:Create(noClipKnob, tweenFast, {Position = UDim2.new(0, 3, 0.5, -7), BackgroundColor3 = Color3.fromRGB(180, 185, 200)}):Play()
        if localPlayer.Character then
            for _, part in ipairs(localPlayer.Character:GetDescendants()) do
                if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
                    part.CanCollide = true
                end
            end
        end
    end
end)

jumpSwitch.MouseButton1Click:Connect(function()
    isJumpEnabled = not isJumpEnabled
    if isJumpEnabled then
        TweenService:Create(jumpSwitch, tweenFast, {BackgroundColor3 = Color3.fromRGB(50, 110, 255)}):Play()
        TweenService:Create(jumpKnob, tweenFast, {Position = UDim2.new(1, -17, 0.5, -7), BackgroundColor3 = Color3.fromRGB(255, 255, 255)}):Play()
    else
        TweenService:Create(jumpSwitch, tweenFast, {BackgroundColor3 = Color3.fromRGB(40, 44, 58)}):Play()
        TweenService:Create(jumpKnob, tweenFast, {Position = UDim2.new(0, 3, 0.5, -7), BackgroundColor3 = Color3.fromRGB(180, 185, 200)}):Play()
    end
end)

espSwitch.MouseButton1Click:Connect(function()
    isEspEnabled = not isEspEnabled
    updateRobloxNametags(isEspEnabled)
    if isEspEnabled then
        TweenService:Create(espSwitch, tweenFast, {BackgroundColor3 = Color3.fromRGB(50, 110, 255)}):Play()
        TweenService:Create(espKnob, tweenFast, {Position = UDim2.new(1, -17, 0.5, -7), BackgroundColor3 = Color3.fromRGB(255, 255, 255)}):Play()
    else
        TweenService:Create(espSwitch, tweenFast, {BackgroundColor3 = Color3.fromRGB(40, 44, 58)}):Play()
        TweenService:Create(espKnob, tweenFast, {Position = UDim2.new(0, 3, 0.5, -7), BackgroundColor3 = Color3.fromRGB(180, 185, 200)}):Play()
    end
end)

-- Обработка прыжка
UserInputService.JumpRequest:Connect(function()
    if isJumpEnabled and localPlayer.Character then
        local humanoid = localPlayer.Character:FindFirstChildOfClass("Humanoid")
        local rootPart = localPlayer.Character:FindFirstChild("HumanoidRootPart")
        if humanoid and rootPart and humanoid:GetState() ~= Enum.HumanoidStateType.Dead then
            local targetJump = tonumber(jumpInput.Text) or 50
            rootPart.AssemblyLinearVelocity = Vector3.new(rootPart.AssemblyLinearVelocity.X, targetJump, rootPart.AssemblyLinearVelocity.Z)
        end
    end
end)

-- Continuous NoClip
RunService.Stepped:Connect(function()
    if isNoClipEnabled and localPlayer.Character then
        for _, part in ipairs(localPlayer.Character:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = false
            end
        end
    end
end)

---------------------------------------------------------
-- ESP СИСТЕМА (3D HIGHLIGHT + ПЛАШКА НАД ГОЛОВОЙ 100x24px)
---------------------------------------------------------
local espContainerMap = {}

local function removePlayerEsp(targetPlayer)
    if espContainerMap[targetPlayer] then
        if espContainerMap[targetPlayer].Highlight then
            espContainerMap[targetPlayer].Highlight:Destroy()
        end
        if espContainerMap[targetPlayer].Billboard then
            espContainerMap[targetPlayer].Billboard:Destroy()
        end
        espContainerMap[targetPlayer] = nil
    end
end

local function setupPlayerEsp(targetPlayer)
    removePlayerEsp(targetPlayer)
    if targetPlayer == localPlayer then return end

    local character = targetPlayer.Character
    if not character then return end

    local hrp = character:FindFirstChild("HumanoidRootPart")
    local head = character:FindFirstChild("Head") or hrp
    if not hrp then return end

    local highlight = Instance.new("Highlight")
    highlight.Name = "LiventyHighlight_" .. targetPlayer.Name
    highlight.Adornee = character
    highlight.FillTransparency = 1
    highlight.OutlineTransparency = 0
    highlight.OutlineColor = currentEspColor
    highlight.Parent = character

    local billboard = Instance.new("BillboardGui")
    billboard.Name = "LiventyTag_" .. targetPlayer.Name
    billboard.AlwaysOnTop = true
    billboard.Size = UDim2.new(0, 100, 0, 24)
    billboard.StudsOffset = Vector3.new(0, 1.8, 0)
    billboard.Adornee = head
    billboard.Parent = head

    local cardFrame = Instance.new("Frame")
    cardFrame.Name = "CardFrame"
    cardFrame.Size = UDim2.new(1, 0, 1, 0)
    cardFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
    cardFrame.BackgroundTransparency = 0.35
    cardFrame.BorderSizePixel = 0
    cardFrame.Parent = billboard

    local cardCorner = Instance.new("UICorner")
    cardCorner.CornerRadius = UDim.new(0, 5)
    cardCorner.Parent = cardFrame

    local cardStroke = Instance.new("UIStroke")
    cardStroke.Name = "CardStroke"
    cardStroke.Thickness = 1.2
    cardStroke.Color = currentEspColor
    cardStroke.Parent = cardFrame

    local nickLabel = Instance.new("TextLabel")
    nickLabel.Name = "NickLabel"
    nickLabel.Size = UDim2.new(0, 56, 0, 11)
    nickLabel.Position = UDim2.new(0, 4, 0, 1)
    nickLabel.BackgroundTransparency = 1
    nickLabel.Text = targetPlayer.DisplayName
    nickLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    nickLabel.TextSize = 10
    nickLabel.Font = Enum.Font.GothamBold
    nickLabel.TextXAlignment = Enum.TextXAlignment.Left
    nickLabel.Parent = cardFrame

    local userLabel = Instance.new("TextLabel")
    userLabel.Name = "UserLabel"
    userLabel.Size = UDim2.new(0, 56, 0, 10)
    userLabel.Position = UDim2.new(0, 4, 0, 12)
    userLabel.BackgroundTransparency = 1
    userLabel.Text = "@" .. targetPlayer.Name
    userLabel.TextColor3 = Color3.fromRGB(200, 205, 220)
    userLabel.TextSize = 8
    userLabel.Font = Enum.Font.Gotham
    userLabel.TextXAlignment = Enum.TextXAlignment.Left
    userLabel.Parent = cardFrame

    local hpLabel = Instance.new("TextLabel")
    hpLabel.Name = "HpLabel"
    hpLabel.Size = UDim2.new(0, 36, 0, 11)
    hpLabel.Position = UDim2.new(0, 60, 0, 1)
    hpLabel.BackgroundTransparency = 1
    hpLabel.Text = "HP-100"
    hpLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    hpLabel.TextSize = 10
    hpLabel.Font = Enum.Font.GothamBold
    hpLabel.TextXAlignment = Enum.TextXAlignment.Right
    hpLabel.Parent = cardFrame

    local distLabel = Instance.new("TextLabel")
    distLabel.Name = "DistLabel"
    distLabel.Size = UDim2.new(0, 36, 0, 10)
    distLabel.Position = UDim2.new(0, 60, 0, 12)
    distLabel.BackgroundTransparency = 1
    distLabel.Text = "0 M"
    distLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    distLabel.TextSize = 8
    distLabel.Font = Enum.Font.GothamBold
    distLabel.TextXAlignment = Enum.TextXAlignment.Right
    distLabel.Parent = cardFrame

    espContainerMap[targetPlayer] = {Highlight = highlight, Billboard = billboard}
end

-- Динамическое обновление ESP
RunService.RenderStepped:Connect(function()
    local localChar = localPlayer.Character
    local localHrp = localChar and localChar:FindFirstChild("HumanoidRootPart")
    local maxDist = tonumber(espDistInput.Text) or 300

    for _, targetPlayer in ipairs(Players:GetPlayers()) do
        if targetPlayer ~= localPlayer then
            local data = espContainerMap[targetPlayer]
            local targetChar = targetPlayer.Character
            local targetHrp = targetChar and targetChar:FindFirstChild("HumanoidRootPart")
            local targetHum = targetChar and targetChar:FindFirstChildOfClass("Humanoid")

            if isEspEnabled and targetChar and targetHrp and targetHum and targetHum.Health > 0 then
                local distance = localHrp and math.floor((localHrp.Position - targetHrp.Position).Magnitude) or 0

                if distance <= maxDist then
                    if not data or not data.Highlight or data.Highlight.Parent ~= targetChar then
                        setupPlayerEsp(targetPlayer)
                        data = espContainerMap[targetPlayer]
                    end

                    if data then
                        data.Highlight.Enabled = true
                        data.Highlight.OutlineColor = currentEspColor

                        data.Billboard.Enabled = true
                        data.Billboard.MaxDistance = maxDist

                        local cardFrame = data.Billboard:FindFirstChild("CardFrame")
                        if cardFrame then
                            cardFrame.CardStroke.Color = currentEspColor
                            cardFrame.NickLabel.Text = targetPlayer.DisplayName
                            cardFrame.UserLabel.Text = "@" .. targetPlayer.Name
                            cardFrame.HpLabel.Text = "HP-" .. math.floor(targetHum.Health)
                            cardFrame.DistLabel.Text = distance .. " M"
                        end

                        targetHum.DisplayDistanceType = Enum.HumanoidDisplayDistanceType.None
                    end
                else
                    if data then
                        data.Highlight.Enabled = false
                        data.Billboard.Enabled = false
                    end
                end
            else
                if data then
                    data.Highlight.Enabled = false
                    data.Billboard.Enabled = false
                end
                if targetHum and not isEspEnabled then
                    targetHum.DisplayDistanceType = Enum.HumanoidDisplayDistanceType.Viewer
                end
            end
        end
    end
end)

Players.PlayerRemoving:Connect(removePlayerEsp)

-- #bypass_physics ПРЕСЛЕДОВАНИЕ ЗА СПИНОЙ СУБ-СМЕЩЕНИЕМ СВЕРХУ И ВЗГЛЯДОМ В ПОЛ
RunService.Heartbeat:Connect(function(deltaTime)
    local character = localPlayer.Character
    if not character then return end

    local rootPart = character:FindFirstChild("HumanoidRootPart")
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if not rootPart or not humanoid then return end

    -- Combat Follow (Преследование выбранного игрока)
    if isCombatFollowActive and selectedCombatPlayer then
        local targetChar = selectedCombatPlayer.Character
        local targetHrp = targetChar and targetChar:FindFirstChild("HumanoidRootPart")
        local targetHum = targetChar and targetChar:FindFirstChildOfClass("Humanoid")

        if targetChar and targetHrp and targetHum and targetHum.Health > 0 then
            -- #stealth позиционирование за спиной сверху со взглядом в сторону/пол цели для хитбокса
            local targetCFrame = targetHrp.CFrame * CFrame.new(0, 2.5, 2)
            rootPart.AssemblyLinearVelocity = targetHrp.AssemblyLinearVelocity
            
            rootPart.CFrame = CFrame.lookAt(targetCFrame.Position, targetHrp.Position)
            humanoid:ChangeState(Enum.HumanoidStateType.Freefall)

            -- Авто-удар ножом если есть оружие
            local tool = character:FindFirstChildOfClass("Tool") or localPlayer.Backpack:FindFirstChildOfClass("Tool")
            if tool then
                if tool.Parent ~= character then
                    humanoid:EquipTool(tool)
                end
                tool:Activate()
            end
        else
            -- #auto_stop отключение преследования при смерти цели
            isCombatFollowActive = false
            humanoid:ChangeState(Enum.HumanoidStateType.GettingUp)
        end
    end

    -- SpeedHack
    if isSpeedEnabled and not isFlyEnabled and not isCombatFollowActive then
        local targetSpeed = tonumber(speedInput.Text) or 50
        local moveDir = humanoid.MoveDirection
        if moveDir.Magnitude > 0 then
            local extraSpeed = math.max(0, targetSpeed - 16)
            rootPart.CFrame = rootPart.CFrame + (moveDir * extraSpeed * deltaTime)
        end
    end

    -- Fly
    if isFlyEnabled and not isCombatFollowActive then
        local flySpeed = tonumber(flySpeedInput.Text) or 50
        humanoid:ChangeState(Enum.HumanoidStateType.Freefall)
        rootPart.AssemblyLinearVelocity = Vector3.zero

        local camCFrame = camera.CFrame
        local forward = camCFrame.LookVector
        local right = camCFrame.RightVector

        local dir = Vector3.zero
        if moveVector.Magnitude > 0.05 then
            dir = (right * moveVector.X) + (forward * -moveVector.Y)
        end

        if dir.Magnitude > 0 then
            rootPart.CFrame = rootPart.CFrame + (dir.Unit * flySpeed * deltaTime)
        end
    end
end)

---------------------------------------------------------
-- LERP DRAG И ОКНО УПРАВЛЕНИЯ
---------------------------------------------------------
local dragging = false
local dragStartPos = Vector2.zero
local frameStartPos = UDim2.new()
local targetPos = mainFrame.Position

headerFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStartPos = Vector2.new(input.Position.X, input.Position.Y)
        frameStartPos = mainFrame.Position

        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = Vector2.new(input.Position.X, input.Position.Y) - dragStartPos
        targetPos = UDim2.new(
            frameStartPos.X.Scale,
            frameStartPos.X.Offset + delta.X,
            frameStartPos.Y.Scale,
            frameStartPos.Y.Offset + delta.Y
        )
    end
end)

RunService.RenderStepped:Connect(function(deltaTime)
    if dragging or (mainFrame.Position ~= targetPos) then
        mainFrame.Position = mainFrame.Position:Lerp(targetPos, math.clamp(deltaTime * 14, 0, 1))
    end
end)

local windowTweenInfo = TweenInfo.new(0.35, Enum.EasingStyle.Quart, Enum.EasingDirection.Out)
local isWindowVisible = true

local function toggleMainWindow()
    isWindowVisible = not isWindowVisible
    if isWindowVisible then
        mainFrame.Visible = true
        TweenService:Create(mainFrame, windowTweenInfo, {Size = UDim2.new(0, 340, 0, 210)}):Play()
    else
        local closeTween = TweenService:Create(mainFrame, windowTweenInfo, {Size = UDim2.new(0, 340, 0, 0)})
        closeTween:Play()
        closeTween.Completed:Connect(function()
            if not isWindowVisible then
                mainFrame.Visible = false
            end
        end)
    end
end

openToggleButton.MouseButton1Click:Connect(toggleMainWindow)

closeButton.MouseButton1Click:Connect(function()
    isFlyEnabled = false
    isSpeedEnabled = false
    isNoClipEnabled = false
    isJumpEnabled = false
    isEspEnabled = false
    isCombatFollowActive = false
    isInstantKillActive = false
    updateRobloxNametags(false)
    for player, data in pairs(espContainerMap) do
        removePlayerEsp(player)
    end
    if joystickFrame then
        joystickFrame:Destroy()
    end
    if quickTargetFrame then
        quickTargetFrame:Destroy()
    end
    screenGui:Destroy()
end)

local isMinimized = false
minimizeButton.MouseButton1Click:Connect(function()
    isMinimized = not isMinimized
    if isMinimized then
        TweenService:Create(mainFrame, windowTweenInfo, {Size = UDim2.new(0, 340, 0, 34)}):Play()
    else
        TweenService:Create(mainFrame, windowTweenInfo, {Size = UDim2.new(0, 340, 0, 210)}):Play()
    end
end)
